interface:
  display_name: "Design System"
  short_description: "Create tailored design tokens and UI systems"
  default_prompt: "Use $design-system to generate a project-specific design system with implementation-ready tokens."
